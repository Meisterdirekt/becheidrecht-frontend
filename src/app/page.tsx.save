'use client';
import React, { useState } from 'react';
import { FileText, Globe } from 'lucide-react';
import FileUpload from '../components/FileUpload';

import { translations } from '../lib/translations';

export default function LandingPage() {
  // Standard-Sprache ist DE
  const [lang, setLang] = useState<'DE' | 'RU' | 'AR' | 'TR' | 'EN'>('DE');
  
  // Hier holen wir uns die Texte basierend auf der gewählten Sprache
  const t = translations[lang];

  return (
    <main className="min-h-screen bg-[#05070a] text-white font-sans">
      {/* Navigation mit funktionierenden Buttons */}
      <nav className="flex justify-between items-center px-8 py-6 max-w-7xl mx-auto">
        <div className="text-2xl font-bold flex items-center gap-2">
          <div className="bg-blue-600 p-1.5 rounded-lg text-white">
            <FileText size={24} />
          </div>
          BescheidRecht
        </div>
        
        <div className="flex gap-4 items-center">
          {/* Sprachumschalter Box */}
          <div className="flex bg-white/5 p-1 rounded-lg border border-white/10">
            {(['DE', 'RU', 'AR', 'TR', 'EN'] as const).map((l) => (
              <button
                key={l}
                onClick={() => {
                  console.log("Sprache gewechselt zu:", l);
                  setLang(l);
                }}
                className={`px-3 py-1 rounded-md text-xs font-bold transition-all ${
                  lang === l ? 'bg-blue-600 text-white shadow-lg' : 'text-gray-500 hover:text-white'
                }`}
              >
                {l}
              </button>
            ))}
          </div>
          <button className="bg-blue-600 hover:bg-blue-700 text-white px-5 py-2 rounded-lg font-bold text-sm">
            Registrieren
          </button>
        </div>
      </nav>

      <header className="pt-24 pb-16 px-4 text-center">
        <h1 className="text-5xl md:text-7xl font-extrabold mb-8 tracking-tight">
          {t.title}
        </h1>
        <p className="text-xl text-gray-400 mb-12 max-w-2xl mx-auto">
          {t.subtitle}
        </p>
        
        {/* Wir geben den übersetzten Text an die Komponente weiter */}
        <FileUpload btnText={t.uploadBtn} analyzingText="Analysiere..." />
      </header>

      {/* Pricing Sektion */}
      <Pricing />

      <footer className="py-10 border-t border-white/10 mt-20 text-center text-gray-600 text-xs px-4">
        <p className="mb-2 italic">{t.legal}</p>
        <p>© 2026 BescheidRecht. Alle Rechte vorbehalten.</p>
      </footer>
    </main>
  );
}
